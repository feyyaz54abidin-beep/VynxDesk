diff --git a/docs/ANDROID_SECURITY_READINESS.md b/docs/ANDROID_SECURITY_READINESS.md
index 5bd51f1..3406639 100644
--- a/docs/ANDROID_SECURITY_READINESS.md
+++ b/docs/ANDROID_SECURITY_READINESS.md
@@ -17,6 +17,15 @@
   are retained as CI artifacts for 30 days (do not treat public-repo artifacts as
   a secret store). Archive maps securely for the supported release lifetime.
 
+## Dependency audit follow-up
+
+The clean-runner audit rejected Starlette 0.50.0 (ten advisory records covering
+five distinct PYSEC IDs). Upgrade the isolated service to FastAPI 0.141.1 and
+Starlette 1.7.0; keep the audit as a blocking gate. Security middleware uses the
+ASGI scope path/scheme, not a URL reconstructed from the untrusted Host header.
+A regression test first reproduced a malformed Host bypass of the HTTPS policy
+on the old dependency stack. It now returns https_required before dispatch.
+
 ## Actual paid-service boundary
 
 `services/licensing` protects enrolled-device inventory with server checks. The
 diff --git a/services/licensing/requirements.txt b/services/licensing/requirements.txt
index ee854d4..278fb0b 100644
--- a/services/licensing/requirements.txt
+++ b/services/licensing/requirements.txt
@@ -1,6 +1,6 @@
 # Revalidate with pip-audit and the test suite before changing these pins.
 cryptography==50.0.1
-fastapi==0.128.2
-starlette==0.50.0
+fastapi==0.141.1
+starlette==1.7.0
 pydantic==2.13.4
 uvicorn==0.48.0
 diff --git a/services/licensing/service.py b/services/licensing/service.py
index 319dc48..cdd7347 100644
--- a/services/licensing/service.py
+++ b/services/licensing/service.py
@@ -341,8 +341,8 @@ def create_app(store: LicenseStore, *, source_url: str | None = None) -> FastAPI
 
     @app.middleware('http')
     async def security(request: Request, call_next):
-        if request.url.path.startswith('/v1/'):
-            if request.url.scheme != 'https':
+        if request.scope.get('path', '').startswith('/v1/'):
+            if request.scope.get('scheme') != 'https':
                 return error(400, 'https_required')
             try:
                 await run_in_threadpool(store.rate_limit, request.client.host if request.client else 'unknown')
 diff --git a/services/licensing/tests/test_service.py b/services/licensing/tests/test_service.py
index 3dc0c10..b506286 100644
--- a/services/licensing/tests/test_service.py
+++ b/services/licensing/tests/test_service.py
@@ -183,6 +183,17 @@ class LicensingTests(unittest.TestCase):
     def test_no_public_provisioning_route(self):
         self.assertEqual(self.client.post('/v1/admin/issue', json={}).status_code, 404)
 
+    def test_malformed_host_cannot_bypass_https_policy(self):
+        insecure = TestClient(self.app, base_url='http://service.test')
+        try:
+            for host in ['service.test/not-v1#', 'service.test/?x=', 'service.test@other.test/']:
+                response = insecure.post('/v1/challenges', headers={'Host': host}, json={
+                    'public_key': self.pub(), 'operation': 'activate', 'binding': 'a' * 64})
+                self.assertEqual(response.status_code, 400, response.text)
+                self.assertEqual(response.json().get('error'), 'https_required')
+        finally:
+            insecure.close()
+
     def test_no_plain_http_for_credentials(self):
         with TestClient(self.app, base_url='http://service.test') as client:
             r = client.post('/v1/challenges', json={})
