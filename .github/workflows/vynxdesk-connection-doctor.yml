name: VYNX Connection Doctor checks

on:
  workflow_dispatch:
  push:
    branches: [master, codex/vynxdesk-connection-doctor-20260926]
    paths:
      - 'flutter/lib/features/connection_doctor/**'
      - 'flutter/test/connection_doctor*'
      - 'flutter/lib/common/widgets/overlay.dart'
      - 'flutter/lib/models/model.dart'
      - 'src/lang/**'
      - 'docs/CONNECTION_DOCTOR*'
      - 'scripts/tests/test_connection_doctor_contract.py'
      - '.github/workflows/vynxdesk-connection-doctor.yml'
  pull_request:
    paths:
      - 'flutter/lib/features/connection_doctor/**'
      - 'flutter/test/connection_doctor*'
      - 'flutter/lib/common/widgets/overlay.dart'
      - 'flutter/lib/models/model.dart'
      - 'src/lang/**'
      - 'docs/CONNECTION_DOCTOR*'
      - 'scripts/tests/test_connection_doctor_contract.py'
      - '.github/workflows/vynxdesk-connection-doctor.yml'

permissions:
  contents: read

concurrency:
  group: vynx-doctor-${{ github.ref }}
  cancel-in-progress: true

jobs:
  feature-and-source:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          persist-credentials: false
          submodules: recursive
      - uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: '3.24.5'
          channel: stable
          cache: true
      - name: Check feature formatting
        run: dart format --output=none --set-exit-if-changed flutter/lib/features/connection_doctor flutter/test/connection_doctor_test.dart flutter/test/connection_doctor_panel_test.dart
      - name: Test exact feature source without native bridge substitutes
        run: |
          set -euo pipefail
          probe="$RUNNER_TEMP/doctor-probe"
          mkdir -p "$probe/lib/features" "$probe/test"
          cp -r flutter/lib/features/connection_doctor "$probe/lib/features/"
          cp flutter/test/connection_doctor*test.dart "$probe/test/"
          printf 'name: flutter_hbb\nenvironment:\n  sdk: ">=3.1.0 <4.0.0"\ndependencies:\n  flutter:\n    sdk: flutter\ndev_dependencies:\n  flutter_test:\n    sdk: flutter\n' > "$probe/pubspec.yaml"
          cd "$probe"
          flutter pub get
          flutter analyze --no-fatal-infos 2>&1 | tee "$RUNNER_TEMP/doctor-analysis.log"
          flutter test --reporter expanded 2>&1 | tee "$RUNNER_TEMP/doctor-tests.log"
      - name: Check source contracts and locale completeness
        run: |
          set -euo pipefail
          python3 -m venv "$RUNNER_TEMP/source-venv"
          "$RUNNER_TEMP/source-venv/bin/pip" install PyYAML==6.0.2
          "$RUNNER_TEMP/source-venv/bin/python" -m unittest discover -s scripts/tests -v 2>&1 | tee "$RUNNER_TEMP/source-tests.log"
          "$RUNNER_TEMP/source-venv/bin/python" scripts/check_build_contract.py
          "$RUNNER_TEMP/source-venv/bin/python" scripts/check_release_contract.py
          python3 scripts/vynx_brand.py --check
      - name: Preserve evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: vynxdesk-connection-doctor-evidence
          path: |
            ${{ runner.temp }}/doctor-analysis.log
            ${{ runner.temp }}/doctor-tests.log
            ${{ runner.temp }}/source-tests.log
          retention-days: 14
          if-no-files-found: warn
